define(['angular.mocks', 'app'], function(mocks, app) {
  'use strict';

  describe('controllers', function(){
    beforeEach(mocks.module('myApp'));

    it('should ....', mocks.inject(function() {
      //spec body
    }));

    it('should ....', mocks.inject(function() {
      //spec body
    }));
  });

});

